From: Your Name <your@email.com>
Date: Sun, 24 May 2026 15:00:00 +0800
Subject: [PATCH] mtd: spinand: fmsh: add FM25G02B ECC status handler

Add ecc_get_status for FM25G02B to fix stability issues.

Signed-off-by: Your Name <your@email.com>
---
 drivers/mtd/nand/spi/fmsh.c | 25 +++++++++++++++++++++++++
 1 file changed, 25 insertions(+)

diff --git a/drivers/mtd/nand/spi/fmsh.c b/drivers/mtd/nand/spi/fmsh.c
index 76f4dbb..9f2d3e8 100644
--- a/drivers/mtd/nand/spi/fmsh.c
+++ b/drivers/mtd/nand/spi/fmsh.c
@@ -86,6 +86,24 @@ static const struct mtd_ooblayout_ops fm25g02b_ooblayout = {
 	.free = fm25g02b_ooblayout_free,
 };
 
+/* FM25G02B ECC status decoder */
+static int fm25g02b_ecc_get_status(struct spinand_device *spinand, u8 status)
+{
+	struct nand_device *nand = spinand_to_nand(spinand);
+	u8 eccsr = (status >> 4) & 0x7;
+	switch (eccsr) {
+	case 0x0: return 0;
+	case 0x1: case 0x2: case 0x3: return 0;
+	case 0x4: return nand->eccreq.strength;
+	case 0x7: return -EBADMSG;
+	default: return -EBADMSG;
+	}
+}
+
 static const struct spinand_info fmsh_spinand_table[] = {
 	SPINAND_INFO("FM25S01A",
 		     SPINAND_ID(SPINAND_READID_METHOD_OPCODE_DUMMY, 0xA1),
@@ -132,7 +150,7 @@ static const struct spinand_info fmsh_spinand_table[] = {
 					      &write_cache_variants,
 					      &update_cache_variants),
 		     SPINAND_HAS_QE_BIT,
-		     SPINAND_ECCINFO(&fm25g02b_ooblayout, NULL)),
+		     SPINAND_ECCINFO(&fm25g02b_ooblayout, fm25g02b_ecc_get_status)),
 	SPINAND_INFO("FM25S02A",
 		     SPINAND_ID(SPINAND_READID_METHOD_OPCODE_DUMMY, 0xE5),
 		     NAND_MEMORG(1, 2048, 64, 64, 2048, 40, 2, 1, 1),
